// Package vst3 defines the plugin-facing parameter surface for a future VST3
// wrapper. It intentionally stays free of host SDK dependencies so the model
// mapping can be tested before the actual plugin bridge exists.
package vst3
